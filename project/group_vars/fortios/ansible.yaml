---
ansible_network_os: "fortios"
ansible_network_provider: "victorock.fortinet_fortios"
ansible_connection: "local"
ansible_gather_facts: false
ansible_provider:
  host: "{{ ansible_host }}"
  username: "{{ ansible_user }}"
  password: "{{ ansible_password }}"
  use_ssl: no