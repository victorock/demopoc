---
- name: "Generate ssh-key"
  run_once: true
  vars:
    ansible_connection: local
    ansible_become: false
  openssh_keypair:
    path: "files/keychain/{{ build_sshkey_name }}"
    type: "rsa"
    mode: 0600